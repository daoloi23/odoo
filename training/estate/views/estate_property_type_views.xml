<odoo>
    <!-- View danh sách cho Property Type -->
    <record id="view_estate_property_type_list" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list default_order="sequence desc, name">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- View biểu mẫu cho Property Type -->
    <record id="view_estate_property_type_form" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box">
                        <button name="action_open_property_ids" type="object" class="oe_stat_button" icon="fa-list">
                            <field name="property_count" widget="line_count"/>

                        </button>
                    </div>
                    <div class="oe_title">
                    <h1>
                        <field name="name"/>
                    </h1>
                    </div>

                    <!-- ✅ Inline list view hiển thị property -->
                    <notebook>
                        <page string="Properties">
                            <field name="property_id">
                                <list editable="bottom">
                                    <field name="name" />
                                    <field name="expected_price"/>
                                    <field name="state"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action mở danh sách Property Types -->
    <record id="action_estate_property_type" model="ir.actions.act_window">
        <field name="name">Estate Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
