<odoo>
<record id="view_estate_property_search" model="ir.ui.view">
    <field name="name">estate.property.search</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
        <search>
            <!-- Search Fields -->

            <field name="name" string="Title" filter_domain="[('name', 'ilike', sefl )]"/>
            <field name="postcode" string="Postcode"/>
            <field name="expected_price" string="Expected Price"/>
            <field name="bedrooms" string="Bedrooms"/>
            <field name="living_area" string="Living Area" filter_domain="[('living_area', '>=', self)]"/>
            <field name="facade" string="Façades"/>
            <field name="property_type_id"/>

            <!-- Filters -->
            <filter name="filter_state_name" string="New or Offer Received" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]"/>
            <filter name="group_by_postcode" string="Group by Postcode" context="{'group_by' : 'postcode'}"/>
        </search>
    </field>
</record>

    <!-- View danh sách -->
<record id="view_estate_property_list" model="ir.ui.view">
    <field name="name">estate.property.list</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
        <list>
            <field name="tag_id" widget="many2many_tags" options="{'color_field': 'color'}" />
            <field name="property_type_id"/>
            <field name="name" string="Title"/>
            <field name="postcode" string="Postcode"/>
            <field name="bedrooms" string="Bedrooms"/>
            <field name="living_area" string="Living Area"/>
            <field name="expected_price" string="Expected Price"/>
            <field name="selling_price" string="Selling Price"/>
            <field name="date_availability" string="Date Availability"/>
        </list>
    </field>
</record>


    <!-- View biểu mẫu -->
    <record id="view_estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <group>
                        <button name="action_sold" type="object" string="Sold" />
                        <button name="action_cancel" type="object" string="Cancel" />
                    </group>
                    <group>
                        <field name="state" widget="statusbar" statusbar_visible="new,offer_received,sold,canceled"/>
                    </group>

                </group>
                <sheet>
                    <!-- Tiêu đề căn nhà -->
                    <h1>
                        <field name="name"/>
                    </h1>
                        <group>
                            <field name="tag_id" widget="many2many_tags" options="{'color_field': 'color'}" />
                        </group>
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': True,'no_edit': True}"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                        </group>
                    </group>
                    <group>
                        <group>
                            <field name="postcode"/>
                        </group>
                        <group>
                            <field name="best_price" readonly="1"/>
                        </group>
                    </group>

                    <!-- Nhóm thông tin tài chính -->
                    <group>
                        <group>
                            <field name="date_availability"/>
                        </group>

                        <group>
                            <field name="selling_price"/>
                        </group>
                    </group>

                    <!-- Tab mô tả -->
                    <notebook>
                        <page string="Description">
                             <group>
                                <field name="description"/>
                            </group>
                            <group>
                                <field name="bedrooms"/>
                            </group>
                            <group>
                                <field name="living_area"/>
                            </group>
                            <group>
                                <field name="facade"/>
                            </group>
                            <group>
                                <field name="garage"/>
                            </group>
                            <group>
                                <field name="garden"/>
                            </group>
                            <group>
                                <field name="garden_area"/>
                            </group>
                            <group>
                                <field name="garden_orientation" widget="radio"/>
                            </group>
                            <group>
                                <group>
                                    <field name="total_area" readonly="1"/>
                                </group>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_id">
                                <list string="Offer" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'" >
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="validity"/>
                                    <field name="date_deadline" column-invisible="parent.state == 'sold'"/>
                                    <button icon="fa-check" type="object" name="action_accept" invisible="status != False and status in ('accepted', 'refused')" />
                                    <button icon="oi-close" type="object" name="action_refuse" invisible="status != False and status in ('accepted', 'refused')" />
                                    <field name="status" options="False"/>
                                    <field name="property_type_id"/>
                                </list>
                            </field>
                        </page>
                        <page string="Other info">
                            <group>
                                <field name="buyer_id"/>
                                <field name="salePerson_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action Properties-->
    <record id="action_estate_property" model="ir.actions.act_window">
        <field name="name">Estate Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_available' : true}</field>
    </record>

    <!-- Action cho estate.property.offer -->
<!--    <record id="action_estate_property_offer" model="ir.actions.act_window">-->
<!--        <field name="name">Estate Property Offers</field>-->
<!--        <field name="res_model">estate.property.offer</field>-->
<!--        <field name="view_mode">list,form</field>-->
<!--    </record>-->
    <!-- Action cho estate.property.tag -->

</odoo>
